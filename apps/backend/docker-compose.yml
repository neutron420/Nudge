version: "3.8"

services:
  backend:
    container_name: nudge-backend
    build:
      context: ../../
      dockerfile: apps/backend/Dockerfile
    ports:
      - "${PORT:-5000}:5000"
    env_file:
      - .env
    restart: always
